{
  "name": "promise-aggregate",
  "version": "0.0.1",
  "description": "promise-aggregator, given an input function `fn` (regular or promise) that is expensive or otherwise needs to be managed, helps control how often `fn` actually executes",
  "main": "promise-aggregate.js",
  "scripts": {
    "test-watch": "mocha tests/index --watch",
    "test": "mocha tests/index"
  },
  "keywords": [
    "promise",
    "throttle",
    "debounce",
    "async",
    "aggregate",
    "aggregator"
  ],
  "author": "fresidue",
  "license": "MIT",
  "dependencies": {
    "delay": "^3.0.0"
  },
  "devDependencies": {
    "lodash": "^4.17.10",
    "mocha": "^5.2.0",
    "time-span": "^2.0.0"
  }
}
